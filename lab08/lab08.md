# Практика 8. Транспортный уровень

## Реализация протокола Stop and Wait (8 баллов)
Реализуйте свой протокол надежной передачи данных типа Stop and Wait на основе ненадежного
транспортного протокола UDP. В вашем протоколе, реализованном на прикладном уровне,
отправитель отправляет пакет (frame) с данными, а затем ожидает подтверждения перед
продолжением.

**Клиент**
- отправляет пакет и ожидает подтверждение ACK от сервера в течение заданного времени (тайм-аута)
- если ACK не получен, пакет отправляется снова
- все пакеты имеют номер (0 или 1) на случай, если один из них потерян

**Сервер**
- ожидает пакеты, отправляет ACK, когда пакет получен
- отправленный ACK должен иметь тот же номер, что и полученный пакет

Вы можете использовать схему, которая была рассмотрена на лекции в рамках обсуждения
протокола rdt3.0, как пример:

<img src="images/rdt.png" width=700 />

### А. Общие требования (5 баллов)
- В качестве базового протокола используйте UDP. Поддержите имитацию 30% потери
  пакетов. Потеря может происходить в обоих направлениях (от клиента серверу и от
  сервера клиенту).
- Должен быть поддержан настраиваемый таймаут.
- Должна быть обработка ошибок (как на сервере, так и на клиенте).
- В качестве демонстрации работоспособности вашего решения передайте через свой
  протокол файл (от клиента на сервер), разбив его на несколько пакетов перед отправкой
  на стороне клиента и собрав из отдельных пакетов в единый файл на стороне сервера.
  Файл и размеры пакетов выберите самостоятельно.

Приложите скриншоты, подтверждающие работоспособность программы.

#### Демонстрация работы
todo

### Б. Дуплексная передача (2 балла)
Поддержите возможность пересылки данных в обоих направлениях: как от клиента к серверу, так и
наоборот. 

Продемонстрируйте передачу файла от сервера клиенту.

#### Демонстрация работы
todo

### В. Контрольные суммы (1 балл)
UDP реализует механизм контрольных сумм при передаче данных. Однако предположим, что
этого нет. Реализуйте и интегрируйте в протокол свой способ проверки корректности данных
на прикладном уровне (для этого вы можете использовать результаты из следующего задания
«Контрольные суммы»).

## Контрольные суммы (2 балла)
Методы, основанные на использовании контрольных сумм, обрабатывают $d$ разрядов данных как
последовательность $k$-разрядных целых чисел.
Наиболее простой метод заключается в простом суммировании этих $k$-разрядных целых чисел и
использовании полученной суммы в качестве битов определения ошибок. Так работает алгоритм
вычисления контрольной суммы, принятый в Интернете, — байты данных группируются в $16$-
разрядные целые числа и суммируются. Затем от суммы берется обратное значение (дополнение
до $1$), которое и помещается в заголовок сегмента.

Получатель проверяет контрольную сумму, складывая все числа из данных (включая контрольную
сумму), и сравнивает результат с числом, все разряды которого равны $1$. Если хотя бы один из
разрядов результата равен $0$, это означает, что произошла ошибка.
В протоколах TCP и UDP контрольная сумма вычисляется по всем полям (включая поля заголовка и
данных).

Реализуйте функцию для подсчета контрольной суммы, а также функцию для проверки, что
данные соответствуют контрольной сумме.

**Требования**
- Функция 1 принимает на вход массив байт и возвращает контрольную сумму (число).
- Функция 2 принимает на вход массив байт и контрольную сумму и проверяет,
соответствует ли сумма переданным данным. Размер входного массива ограничен сверху
числом байтов ($= L$), однако данные могут поступать разной длины ($\le L$).

Добавьте два-три теста, покрывающих как случаи
корректной работы, так и случаи ошибки в данных (сбой битов). Вы можете не использовать
тестовые фреймворки и реализовать тестовые сценарии в консольном приложении.

## Задачи

### Задача 1 (2 балла)
Пусть $T$ (измеряется в RTT) обозначает интервал времени, который TCP-соединение тратит на
увеличение размера окна перегрузки с $\frac{W}{2}$ до $W$, где $W$ – это максимальный размер окна
перегрузки. Докажите, что $T$ – это функция от средней пропускной способности TCP.

#### Решение
Рассмотрим увеличение cwnd с $\frac{W}{2}$ до $W$. По времени оно занимает $T \cdot RTT$, то есть $T$ раз происходит аддитивное увеличение: мы видим что все пакеты которые мы отправляли были успешно получены и увеличиваем окно на $MSS$. То есть всего размер окна увеличится на $T \cdot MSS$. Таким образом, $\frac{W}{2} + T \cdot MSS = W \Rightarrow T \cdot MSS = \frac{W}{2} \Rightarrow T = \frac{W}{2 \cdot MSS} = \frac{\frac{3}{4} W}{\frac{3}{2} MSS}$. $\frac{3}{4} W$ это средняя пропускная способность, знаменатель константа, то есть $T$ это действительно функция от пропускной способности.

### Задача 2 (3 балла)
Рассмотрим задержку, полученную в фазе медленного старта TCP. Рассмотрим клиент и веб-сервер, напрямую соединенные одним каналом со скоростью передачи данных $R$.
Предположим, клиент хочет получить от сервера объект, размер которого точно равен $15 \cdot S$,
где $S$ – это максимальный размер сегмента.
Игнорируя заголовки протокола, определите время извлечения объекта (общее время
задержки), включая время на установление TCP-соединения (предполагается, что RTT - константа), если:
1. $\dfrac{4S}{R} > \dfrac{S}{R} + RTT > \dfrac{2S}{R}$
2. $\dfrac{𝑆}{𝑅} + 𝑅𝑇𝑇 > \dfrac{4𝑆}{𝑅}$
3. $\dfrac{𝑆}{𝑅} > 𝑅𝑇𝑇$

#### Решение
Оправка одного пакета размера $S$ с сервера на клиент занимает $d_{propagation} + d_{transmission} = 0.5 RTT + \frac{S}{R}$, отправка клиентом ACK-пакета в ответ на него займёт $0.5 RTT$ (пренебрегаем размером ACK-пакета потому что в нём только заголовок). Чтобы выбрать значение таймаута, сервер пытается оценить RTT, видит что ответ на отправленный им пакет приходит через $0.5 RTT + \frac{S}{R} + 0.5 RTT = RTT + \frac{S}{R}$ и устанавливает таймаут приблизительно равным $RTT + \frac{S}{R}$. То есть 3 случая в условии задачи это на самом деле

1. $\dfrac{4S}{R} > timeout > \dfrac{2S}{R}$
2. $timeout > \dfrac{4S}{R}$
3. $timeout < \dfrac{2S}{R}$

Начинается передача во всех трёх случаях одинаково:

- Время: 0.

Клиент отправляет SYN.

- Время: $0.5 RTT$.

Сервер получает SYN, отправляет SYNACK.

- Время: $RTT$.

Клиент получает SYNACK, отправляет ACK.

- Время: $1.5 RTT$.

Сервер получает ACK, TCP-соединение установлено. Сервер ставит $cwnd = S$ и начинает фазу медленного старта. Отправляет 1-й пакет с данными (размера $S$).

- Время: $2 RTT + \frac{S}{R}$.

Клиент получает 1-й пакет с данными, отправляет ACK на него.

- Время: $2.5 RTT + \frac{S}{R}$.

Сервер получает ACK, увеличивает $cwnd$, теперь $cwnd = 2S$. Отправляет 2-й и 3-й пакеты с данными.

- Время: $3 RTT + \frac{2S}{R}$.

Клиент получает 2-й пакет с данными, отправляет ACK.

- Время: $3 RTT + \frac{3S}{R}$.

Клиент получает 3-й пакет с данными, отправляет ACK.

- Время: $3.5 RTT + \frac{2S}{R}$.

Сервер получает ACK на 2-й пакет с данными, увеличивает $cwnd$, теперь $cwnd = 3S$. Отправляет 4-й и 5-й пакеты с данными.

Дальше случаи начинают отличаться.

#### Первый и второй случаи, $timeout > \dfrac{2S}{R}$

- Время: $3.5 RTT + \frac{3S}{R}$.

Сервер получает ACK на 3-й пакет с данными, увеличивает $cwnd$, теперь $cwnd = 4S$. Отправляет 6-й и 7-й пакеты с данными.

Время: $4 RTT + \frac{3S}{R}$.

Клиент получает 4-й пакет с данными, отправляет ACK.

- Время: $4 RTT + \frac{4S}{R}$.

Клиент получает 5-й пакет с данными, отправляет ACK.

Время: $4 RTT + \frac{5S}{R}$.

Клиент получает 6-й пакет с данными, отправляет ACK.

- Время: $4 RTT + \frac{6S}{R}$.

Клиент получает 7-й пакет с данными, отправляет ACK.

- Время: $4.5 RTT + \frac{3S}{R}$.

Сервер получает ACK на 4-й пакет с данными, увеличивает $cwnd$, теперь $cwnd = 5S$. Отправляет 8-й и 9-й пакеты с данными.

- Время: $4.5 RTT + \frac{4S}{R}$.

Сервер получает ACK на 5-й пакет с данными, увеличивает $cwnd$, теперь $cwnd = 6S$. Отправляет 10-й и 11-й пакеты с данными.

- Время: $4.5 RTT + \frac{5S}{R}$.

Сервер получает ACK на 6-й пакет с данными, увеличивает $cwnd$, теперь $cwnd = 7S$. Отправляет 12-й и 13-й пакеты с данными.

- Время: $4.5 RTT + \frac{6S}{R}$.

Сервер получает ACK на 7-й пакет с данными, увеличивает $cwnd$, теперь $cwnd = 8S$. Отправляет 14-й и 15-й пакеты с данными.

- Время: $5 RTT + \frac{4S}{R}$.

Клиент получает 8-й пакет с данными, отправляет ACK.

- Время: $5 RTT + \frac{5S}{R}$.

Клиент получает 9-й пакет с данными, отправляет ACK.

- Время: $5 RTT + \frac{6S}{R}$.

Клиент получает 10-й пакет с данными, отправляет ACK.

- Время: $5 RTT + \frac{7S}{R}$.

Клиент получает 11-й пакет с данными, отправляет ACK.

- Время: $5 RTT + \frac{8S}{R}$.

Клиент получает 12-й пакет с данными, отправляет ACK.

- Время: $5 RTT + \frac{9S}{R}$.

Клиент получает 13-й пакет с данными, отправляет ACK.

- Время: $5 RTT + \frac{10S}{R}$.

Клиент получает 14-й пакет с данными, отправляет ACK.

- Время: $5 RTT + \frac{11S}{R}$.

Клиент получает 15-й пакет с данными, отправляет ACK.

- Время: $5.5 RTT + \frac{4S}{R}$

Сервер получает ACK на 8-й пакет.

- Время: $5.5 RTT + \frac{5S}{R}$

Сервер получает ACK на 9-й пакет.

- Время: $5.5 RTT + \frac{6S}{R}$

Сервер получает ACK на 10-й пакет.

- Время: $5.5 RTT + \frac{7S}{R}$

Сервер получает ACK на 11-й пакет.

- Время: $5.5 RTT + \frac{8S}{R}$

Сервер получает ACK на 12-й пакет.

Тут первый случай ($timeout < \dfrac{4S}{R}$) начинает отличаться от второго ($timeout > \dfrac{4S}{R}$).

#### Первый случай, $\dfrac{2S}{R} < timeout < \dfrac{4S}{R}$

- Время: $4.5 RTT + \frac{5S}{R} + timeout$.

У сервера истекает таймаут на 13-й пакет ($timeout < \frac{4S}{R}$, поэтому $4.5 RTT + \frac{5S}{R} + timeout < 5.5 RTT + \frac{9S}{R}$, то есть ACK на него ещё не пришёл). Уменьшаем $cwnd$ в 2 раза и выходим из медленного старта. Уже получили ACK-пакеты на все пакеты с данными, кроме трёх последних (13-го, 14-го, и 15-го). Отправляем 13-й пакет повторно.

- Время: $5.5 RTT + \frac{9S}{R}$

Сервер получает ACK на 13-й пакет.

- Время: $4.5 RTT + \frac{6S}{R} + timeout$.

У сервера истекает таймаут на 14-й пакет, отправляет 14-й пакет повторно.

- Время: $5.5 RTT + \frac{10S}{R}$

Сервер получает ACK на 14-й пакет.

- Время: $4.5 RTT + \frac{7S}{R} + timeout$.

У сервера истекает таймаут на 15-й пакет, отправляет 15-й пакет повторно.

- Время: $5.5 RTT + \frac{11S}{R}$

Сервер получает ACK на 15-й пакет. Передача закончена.

#### Второй случай, $timeout > \dfrac{4S}{R}$

- Время: $5.5 RTT + \frac{9S}{R}$

Сервер получает ACK на 13-й пакет.

- Время: $5.5 RTT + \frac{10S}{R}$

Сервер получает ACK на 14-й пакет.

- Время: $5.5 RTT + \frac{11S}{R}$

Сервер получает ACK на 15-й пакет. Передача закончена.

Осталось рассмотреть 3-й случай.

#### Третий случай, $timeout < \dfrac{2S}{R}$

- Время: $2.5 RTT + \frac{S}{R} + timeout$.

У сервера истекает таймаут на 3-й пакет ($timeout < \frac{2S}{R}$, поэтому $2.5 RTT + \frac{S}{R} + timeout < 3.5 RTT + \frac{3S}{R}$, то есть ACK на него ещё не пришёл). Уменьшаем $cwnd$ в 2 раза и выходим из медленного старта. Теперь $cwnd = S$. Отправляем 3-й пакет повторно.

- Время: $3.5 RTT + \frac{3S}{R}$.

Сервер получает ACK на 3-й пакет с данными, отправляет 4-й пакет с данными.

- Время: $3 RTT + \frac{2S}{R} + timeout$.

Клиент получает повторно отправленный 3-й пакет, оправляет ещё один ACK на него.

- Время: $4 RTT + \frac{4S}{R}$.

Клиент получает 4-й пакет, отправляет ACK.

- Время: $4.5 RTT + \frac{4S}{R}$.

Сервер получает ACK на 4-й пакет с данными. За последний RTT потерь пакетов не было, поэтому увеличиваем $cwnd$ на 1, теперь $cwnd = 2S$. Отправляем 5-й и 6-й пакеты с данными.

- Время: $5 RTT + \frac{5S}{R}$.

Клиент получает 5-й пакет, отправляет ACK.

- Время: $5 RTT + \frac{6S}{R}$.

Клиент получает 6-й пакет, отправляет ACK.

- Время: $5.5 RTT + \frac{5S}{R}$

Сервер получает ACK на 5-й пакет, отправляет 7-й пакет.

- Время: $4.5 RTT + \frac{4S}{R} + timeout$

У сервера истёк таймаут на 6-й пакет, уменьшаем $cwnd$ в 2 раза, теперь $cwnd = S$. Отправляем 6-й пакет повторно.

- Время: $5.5 RTT + \frac{6S}{R}$

Сервер получает ACK на 6-й пакет, ничего не отправляет, так как $cwnd = S$ и 7-й пакет уже отправлен, ждём ACK на него прежде чем отправлять дальше.

- Время: $6 RTT + \frac{6S}{R}$.

Клиент получает 7-й пакет, отправляет ACK.

- Время: $5 RTT + \frac{4S}{R} + timeout$.

Клиент получает повторно отправленный 6-й пакет, оправляет ещё один ACK на него.

- Время: $6.5 RTT + \frac{6S}{R}$.

Сервер получает ACK на 7-й пакет, отправляет 8-й пакет.

- Время: $7 RTT + \frac{7S}{R}$.

Клиент получает 8-й пакет, отправляет ACK.

- Время: $7.5 RTT + \frac{7S}{R}$

Сервер получает ACK на 8-й пакет с данными. За последний RTT потерь пакетов не было, поэтому увеличиваем $cwnd$ на 1, теперь $cwnd = 2S$. Отправляем 9-й и 10-й пакеты с данными.

- Время: $8 RTT + \frac{8S}{R}$

Клиент получает 9-й пакет, отправляет ACK.

- Время: $8 RTT + \frac{9S}{R}$

Клиент получает 10-й пакет, отправляет ACK.

- Время: $8.5 RTT + \frac{8S}{R}$

Сервер получает ACK на 9-й пакет, отправляет 11-й пакет.

- Время: $7.5 RTT + \frac{7S}{R} + timeout$

У сервера истёк таймаут на 10-й пакет, уменьшаем $cwnd$ в 2 раза, теперь $cwnd = S$. Отправляем 10-й пакет повторно.

- Время: $8.5 RTT + \frac{9S}{R}$

Сервер получает ACK на 10-й пакет, ничего не отправляет, так как $cwnd = S$ и 11-й пакет уже отправлен, ждём ACK на него прежде чем отправлять дальше.

- Время: $9 RTT + \frac{9S}{R}$.

Клиент получает 11-й пакет, отправляет ACK.

- Время: $8 RTT + \frac{8S}{R} + timeout$.

Клиент получает повторно отправленный 10-й пакет, оправляет ещё один ACK на него.

- Время: $8.5 RTT + \frac{9S}{R}$.

Сервер получает ACK на 11-й пакет, отправляет 12-й пакет.

- Время: $9 RTT + \frac{10S}{R}$.

Клиент получает 12-й пакет, отправляет ACK.

- Время: $9.5 RTT + \frac{10S}{R}$

Сервер получает ACK на 12-й пакет с данными. За последний RTT потерь пакетов не было, поэтому увеличиваем $cwnd$ на 1, теперь $cwnd = 2S$. Отправляем 13-й и 14-й пакеты с данными.

- Время: $10 RTT + \frac{11S}{R}$

Клиент получает 13-й пакет, отправляет ACK.

- Время: $10 RTT + \frac{12S}{R}$

Клиент получает 14-й пакет, отправляет ACK.

- Время: $10.5 RTT + \frac{11S}{R}$

Сервер получает ACK на 13-й пакет, отправляет 15-й пакет.

- Время: $9.5 RTT + \frac{10S}{R} + timeout$

У сервера истёк таймаут на 14-й пакет, уменьшаем $cwnd$ в 2 раза, теперь $cwnd = S$. Отправляем 14-й пакет повторно.

- Время: $10.5 RTT + \frac{12S}{R}$

Сервер получает ACK на 14-й пакет.

- Время: $11 RTT + \frac{12S}{R}$.

Клиент получает 15-й пакет, отправляет ACK.

- Время: $10 RTT + \frac{11S}{R} + timeout$.

Клиент получает повторно отправленный 14-й пакет, оправляет ещё один ACK на него.

- Время: $11.5 RTT + \frac{12S}{R}$.

Сервер получает ACK на 15-й пакет. Передача закончена.



Получили, что в первлм и втором случаях передача данных заняла $5.5 RTT + \frac{11S}{R}$, а в третьем случае $11.5 RTT + \frac{12S}{R}$. В первом случае, в отличие от второго, начали истекать таймауты у пакетов, но это произошло уже после того как все пакеты были отправлены и поэтому не повлияло на общее время. В третьем случае таймауты начали истекать почти в самом начале передачи, поэтому она заняла больше времени.

<!--Поскольку все 15 пакетов к этому моменту уже отправлены, это ни на что не повлияет.
Сервер получает ACK на 8-й пакет с данными, увеличивает $cwnd$, теперь $cwnd = 8S$. Отправляет 14-й и 15-й пакеты с данными.-->

### Задача 3 (2 балла)
Рассмотрим модификацию алгоритма управления перегрузкой протокола TCP. Вместо
аддитивного увеличения, мы можем использовать мультипликативное увеличение. TCP-отправитель увеличивает размер своего окна в $(1 + a)$ раз (где $a$ - небольшая положительная
константа: $0 < a < 1$), как только получает верный ACK-пакет.
Найдите функциональную зависимость между частотой потерь $L$ и максимальным размером окна
перегрузки $W$. Утверждается, что для этого измененного протокола TCP, независимо от средней
пропускной способности TCP-соединения всегда требуется одинаковое количество времени для
увеличения размера окна перегрузки с $\frac{W}{2}$ до $W$.

#### Решение
Увеличение окна с $\frac{W}{2}$ до $W$ это $\log_{1 + a}{2}$ увеличений в $1 + a$ раз. Это может нецелым числом, если это так, между двумя соседними мультипликативными уменьшениями всё равно будет в среднем $\log_{1 + a}{2}$ увеличений (потому что <!--пропускная способность всё равно в среднем держится около $\frac{3}{4} W$, а--> если бы на одно уменьшение приходилось бы в среднем любое другое количество увеличений, пропускная способность не была бы асимптотически константой (стремилась бы к нулю или неограниченно росла)). <!--Если на $1$ уменьшение в среднем приходится $\log_{1 + a}{2}$ увеличений, то на $1$ потерянный пакет в среднем приходится $\log_{1 + a}{2}$ успешно доставленных, то есть частота потерь $L = \frac{1}{1 + \log_{1 + a}{2}}$. Эта величина не зависит от средней пропускной способности $\frac{3}{4} W$, а зависит только от выбора константы $a$.

Увеличение окна перегрузки с $\frac{W}{2}$ до $W$ в среднем занимает $\log_{1 + a}{2}$ увеличений, это $\log_{1 + a}{2}$ полученных верных ACK-пакетов. Поскольку в среднем мы получаем $\frac{3}{4} W$ пакетов за $RTT$, по времени это будет $\frac{\log_{1 + a}{2}}{\frac{3}{4} W} RTT$.-->

Это значит что потеря пакета происходит в среднем один раз за $(1 + \log_{1 + a}{2}) RTT$ (1 RTT на 1 уменьшение окна, $\log_{1 + a}{2} \cdot RTT$ на $\log_{1 + a}{2}$ его увеличений). Средняя пропускная способность равна $\frac{3}{4} W$, то есть за $(1 + \log_{1 + a}{2}) RTT$ в среднем передаётся $\frac{3}{4} W \cdot (1 + \log_{1 + a}{2}) RTT$ пакетов. То есть на один потерянный пакет в среднем приходится $\frac{3}{4} W \cdot (1 + \log_{1 + a}{2}) RTT$ успешно переданных. Таким образом, частота потерь $L = \frac{1}{1 + \frac{3}{4} W \cdot (1 + \log_{1 + a}{2}) RTT}$.

По времени увеличение окна с $\frac{W}{2}$ до $W$ будет занимать в среднем $(1 + \log_{1 + a}{2}) RTT$, что не зависит от $W$ и, соответственно, от пропускной способности $\frac{3}{4} W$.

### Задача 4. Расслоение TCP (2 балла)
Для облачных сервисов, таких как поисковые системы, электронная почта и социальные сети,
желательно обеспечить малое время отклика если конечная система расположена далеко от датацентра, то значение RTT будет большим, что может привести к неудовлетворительному времени
отклика, связанному с этапом медленного старта протокола TCP.
Рассмотрим задержку получения ответа на поисковый запрос. Обычно серверу требуется три окна
TCP на этапе медленного старта для доставки ответа. Таким образом, время с момента, когда
конечная система инициировала TCP-соединение, до времени, когда она получила последний
пакет в ответ, составляет примерно $4$ RTT (один RTT для установления TCP-соединения, плюс три
RTT для трех окон данных) плюс время обработки в дата-центре. Такие RTT задержки могут
привести к заметно замедленной выдаче результатов поиска для многих запросов. Более того,
могут присутствовать также и значительные потери пакетов в сетях доступа, приводящие к
повторной передаче TCP и еще большим задержкам.

Один из способов смягчения этой проблемы и улучшения восприятия пользователем
производительности заключается в том, чтобы:
- развернуть внешние серверы ближе к пользователям
- использовать расслоение TCP путем разделения TCP-соединения на внешнем сервере.
При расслоении клиент устанавливает TCP-соединение с ближайшим внешним сервером, который
поддерживает постоянное TCP-соединение с дата-центром с очень большим окном перегрузки TCP.

При использовании такого подхода время отклика примерно равно:
$$4 \cdot RTT_{FE} + RTT_{BE} + \text{ время обработки}~~~~~~~(1)$$
где $RTT_{FE}$ — время оборота между клиентом и внешним сервером, и $RTT_{BE}$ — время оборота
между внешним сервером и дата-центром (внутренним сервером). Если внешний сервер закрыт
для клиента, то это время ответа приближается к $RTT$ плюс время обработки, поскольку значение
$RTT_{FE}$ ничтожно мало и значение $RTT_{BE}$ приблизительно равно $RTT$. В итоге расслоение TCP
может уменьшить сетевую задержку, грубо говоря, с $4 \cdot RTT$ до $RTT$, значительно повышая
субъективную производительность, особенно для пользователей, которые расположены далеко
от ближайшего дата-центра.

Расслоение TCP также помогает сократить задержку повторной передачи TCP, вызванную
потерями в сетях.

Докажите утверждение $(1)$. 

#### Решение
todo
 